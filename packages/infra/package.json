{
  "name": "@corporation/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rm -rf .alchemy/pids && alchemy dev",
    "check-types": "tsc --noEmit",
    "deploy": "alchemy deploy",
    "destroy": "ALLOW_MISSING_PREVIEW_CONVEX=1 alchemy destroy"
  },
  "dependencies": {
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@corporation/config": "workspace:*",
    "@types/node": "catalog:",
    "alchemy": "catalog:",
    "typescript": "catalog:"
  }
}
