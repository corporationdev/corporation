{
  "name": "@corporation/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "dev": "STAGE_VALUE=${STAGE:-$(awk -F= '/^STAGE=/{print $2; exit}' .env 2>/dev/null)}; case \"$STAGE_VALUE\" in sandbox|sandbox-*) CONVEX_AGENT_MODE=anonymous convex dev --local ;; *) convex dev ;; esac",
    "dev:setup": "convex dev --configure --until-success"
  },
  "dependencies": {
    "@convex-dev/better-auth": "catalog:",
    "e2b": "catalog:",
    "@nangohq/node": "catalog:",
    "better-auth": "catalog:",
    "convex": "catalog:",
    "convex-helpers": "catalog:",
    "dotenv": "catalog:",
    "svix": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@corporation/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
