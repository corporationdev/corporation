#!/usr/bin/env bash
# One-off sandbox setup commands for dev syncing.
# Add commands here when you need something on an existing sandbox
# without rebuilding the base template. Once you rebuild the template
# with the command baked in, remove it from here.
set -euo pipefail

# Example:
# apt-get update -qq && apt-get install -y -qq some-package

echo "Sandbox setup complete"
