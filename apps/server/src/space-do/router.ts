// Raw DO websocket/HTTP routing lives in object.ts.
//
// Keep this file so the migration layout matches the intended architecture and
// we have a stable home for follow-up protocol extraction.
export {};
