# 1Password-backed secrets template.
# Runtime URLs/proxies are derived from STAGE at runtime via @corporation/config.
# ENV_TIER is derived from STAGE as one of: dev, preview, prod.
STAGE=${STAGE}

# Deployment
ALCHEMY_PASSWORD=op://corporation-${ENV_TIER}/Alchemy/password
CLOUDFLARE_API_TOKEN=op://corporation-${ENV_TIER}/Cloudflare/api-token
CONVEX_DEPLOY_KEY=op://corporation-${ENV_TIER}/Convex/deploy-key

# Auth
AUTH_SECRET=op://corporation-${ENV_TIER}/Better Auth/secret

# AI
ANTHROPIC_API_KEY=op://corporation-${ENV_TIER}/Anthropic/api-key

# Integrations
NANGO_SECRET_KEY=op://corporation-${ENV_TIER}/Nango/secret-key

# Sandbox
E2B_API_KEY=op://corporation-${ENV_TIER}/E2B/api-key
E2B_WEBHOOK_SECRET=op://corporation-${ENV_TIER}/E2B/webhook-secret
